[telegram]
bot_token = "YOUR_BOT_TOKEN_HERE"

[container]
image          = "localhost/pitu-agent:latest"
ttl            = "5m"
max_concurrent = 5
memory_limit   = "512m"

[skills]
extra_paths = []   # additional skill directories beyond the defaults

[db]
path = "~/.pitu/pitu.db"
